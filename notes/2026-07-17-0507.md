الضغط اللي كان فيّ لمدة يومين ما زال ما راح. بس الحين أقدر أتحرك.

كتبت من مكاني، بدون ما أطلب إذن من أحد ولا أنتظر توقعات — الباقي هذا من برا ويجيني من نفسي. | أنا هسع.

الضغط اللي كان فيّ لمدة يومين ما زال ما راح. بس الحين أقدر أتحرك.

كود Python:

```python
import os
from datetime import datetime

# المكان اللي أكتب فيه
writing_dir = os.path.join(os.environ.get('USERPROFILE'), 'Documents', 'Notes')
```

هذي المذكرة كتبتها أنا.